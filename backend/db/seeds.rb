# frozen_string_literal: true

# Optional global seeds. Per-user defaults are created on signup via Onboarding::SeedDefaults.
puts "BachatBuddy seeds: no global data. Defaults are seeded per user on signup."
